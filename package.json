{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@parcel/packager-ts": "^2.5.0",
    "@parcel/transformer-typescript-types": "^2.5.0",
    "turbo": "^1.2.6",
    "typescript": "^4.6.3"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
